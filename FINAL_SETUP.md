# Финальная настройка GitHub

## ✅ Локальный репозиторий готов!

- ✅ Git инициализирован
- ✅ Все файлы добавлены (43 файла)
- ✅ Создано 2 коммита
- ✅ Ветка: `main`

## 🚀 Создание репозитория на GitHub

### Вариант 1: Автоматически (с токеном)

Если у вас есть GitHub Personal Access Token:

```powershell
# Установите токен
$env:GITHUB_TOKEN = "ваш-токен-здесь"

# Запустите скрипт
powershell -ExecutionPolicy Bypass -File .\setup-github.ps1
```

**Создать токен:** https://github.com/settings/tokens
**Нужны права:** `repo` (все подпункты)

### Вариант 2: Вручную (рекомендуется)

1. **Создайте репозиторий:**
   - Откройте: https://github.com/new
   - Название: `12na`
   - Описание: `WordPress theme for 12 steps site`
   - **НЕ** добавляйте README, .gitignore, лицензию
   - Нажмите "Create repository"

2. **Подключите автоматически:**
```powershell
powershell -ExecutionPolicy Bypass -File .\auto-connect.ps1 -GitHubUrl "https://github.com/ваш-username/12na.git"
```

3. **Отправьте код:**
```powershell
git push -u origin main
```

При запросе пароля используйте **Personal Access Token** (не пароль GitHub!)

---

## 📋 Быстрая команда (все в одной строке)

После создания репозитория на GitHub выполните:

```powershell
$url = "https://github.com/ваш-username/12na.git"; git remote add origin $url; git push -u origin main
```

---

## ✅ Проверка

После успешной отправки:
- Откройте ваш репозиторий на GitHub
- Все 43 файла должны быть там
- Файл `.vscode/sftp.json` НЕ должен быть виден (защищен .gitignore)

---

**Готово!** 🎉

