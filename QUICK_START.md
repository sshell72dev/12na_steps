# Быстрый старт - подключение к GitHub

## ✅ Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены в Git
3. ✅ Первый коммит создан
4. ✅ Ветка переименована в `main`
5. ✅ `.gitignore` настроен (пароли не попадут в Git)

## 🚀 Финальный шаг - подключение к GitHub:

### Вариант 1: Использовать скрипт (рекомендуется)

1. Создайте репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Название: `12na` (или любое другое)
   - НЕ добавляйте README, .gitignore или лицензию
   - Нажмите "Create repository"

2. Скопируйте URL репозитория (например: `https://github.com/ваш-username/12na.git`)

3. Запустите скрипт:
```powershell
.\connect-to-github.ps1 -GitHubUrl "https://github.com/ваш-username/12na.git"
```

4. Отправьте код:
```powershell
git push -u origin main
```

### Вариант 2: Вручную

1. Создайте репозиторий на GitHub (см. выше)

2. Выполните команды:
```powershell
git remote add origin https://github.com/ваш-username/12na.git
git push -u origin main
```

## 🔐 Авторизация

При `git push` GitHub запросит авторизацию:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Дайте имя токену
4. Выберите права: **repo** (все подпункты)
5. Нажмите "Generate token"
6. **Скопируйте токен** (показывается только один раз!)
7. При запросе пароля введите этот токен

## 📝 Проверка

После успешной отправки:
- Откройте ваш репозиторий на GitHub
- Все файлы должны быть там
- Файл `.vscode/sftp.json` НЕ должен быть виден (защищен .gitignore)

---

**Готово!** 🎉 Ваш проект теперь на GitHub!

